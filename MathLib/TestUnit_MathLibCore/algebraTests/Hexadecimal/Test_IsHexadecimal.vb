Namespace Algebra.Hexadecimal

    <TestClass()>
  Public Class Test_IsHexadecimal

        <TestMethod()>
    <Description("0")> _
        Public Sub Test_01()

            Dim Msg As String
            Dim Value As String

            '-- Initialisation des valeurs --------------------------------------
            Value = "0"
            '--------------------------------------------------------------------

            Msg = "Value = " & Value
            Assert.IsTrue(MathLib.Algebra.Hexadecimal.IsHex(Value), Msg)

        End Sub

        <TestMethod()>
    <Description("1")> _
        Public Sub Test_02()

            Dim Msg As String
            Dim Value As String

            '-- Initialisation des valeurs --------------------------------------
            Value = "1"
            '--------------------------------------------------------------------

            Msg = "Value = " & Value
            Assert.IsTrue(MathLib.Algebra.Hexadecimal.IsHex(Value), Msg)

        End Sub

        <TestMethod()>
    <Description("2")> _
        Public Sub Test_03()

            Dim Msg As String
            Dim Value As String

            '-- Initialisation des valeurs --------------------------------------
            Value = "2"
            '--------------------------------------------------------------------

            Msg = "Value = " & Value
            Assert.IsTrue(MathLib.Algebra.Hexadecimal.IsHex(Value), Msg)

        End Sub

        <TestMethod()>
    <Description("3")> _
        Public Sub Test_04()

            Dim Msg As String
            Dim Value As String

            '-- Initialisation des valeurs --------------------------------------
            Value = "3"
            '--------------------------------------------------------------------

            Msg = "Value = " & Value
            Assert.IsTrue(MathLib.Algebra.Hexadecimal.IsHex(Value), Msg)

        End Sub

        <TestMethod()>
    <Description("4")> _
        Public Sub Test_05()

            Dim Msg As String
            Dim Value As String

            '-- Initialisation des valeurs --------------------------------------
            Value = "4"
            '--------------------------------------------------------------------

            Msg = "Value = " & Value
            Assert.IsTrue(MathLib.Algebra.Hexadecimal.IsHex(Value), Msg)

        End Sub

        <TestMethod()>
    <Description("5")> _
        Public Sub Test_06()

            Dim Msg As String
            Dim Value As String

            '-- Initialisation des valeurs --------------------------------------
            Value = "5"
            '--------------------------------------------------------------------

            Msg = "Value = " & Value
            Assert.IsTrue(MathLib.Algebra.Hexadecimal.IsHex(Value), Msg)

        End Sub

        <TestMethod()>
    <Description("6")> _
        Public Sub Test_07()

            Dim Msg As String
            Dim Value As String

            '-- Initialisation des valeurs --------------------------------------
            Value = "6"
            '--------------------------------------------------------------------

            Msg = "Value = " & Value
            Assert.IsTrue(MathLib.Algebra.Hexadecimal.IsHex(Value), Msg)

        End Sub

        <TestMethod()>
    <Description("7")> _
        Public Sub Test_08()

            Dim Msg As String
            Dim Value As String

            '-- Initialisation des valeurs --------------------------------------
            Value = "7"
            '--------------------------------------------------------------------

            Msg = "Value = " & Value
            Assert.IsTrue(MathLib.Algebra.Hexadecimal.IsHex(Value), Msg)

        End Sub

        <TestMethod()>
    <Description("8")> _
        Public Sub Test_09()

            Dim Msg As String
            Dim Value As String

            '-- Initialisation des valeurs --------------------------------------
            Value = "8"
            '--------------------------------------------------------------------

            Msg = "Value = " & Value
            Assert.IsTrue(MathLib.Algebra.Hexadecimal.IsHex(Value), Msg)

        End Sub

        <TestMethod()>
    <Description("9")> _
        Public Sub Test_10()

            Dim Msg As String
            Dim Value As String

            '-- Initialisation des valeurs --------------------------------------
            Value = "9"
            '--------------------------------------------------------------------

            Msg = "Value = " & Value
            Assert.IsTrue(MathLib.Algebra.Hexadecimal.IsHex(Value), Msg)

        End Sub

        <TestMethod()>
    <Description("a")> _
        Public Sub Test_11()

            Dim Msg As String
            Dim Value As String

            '-- Initialisation des valeurs --------------------------------------
            Value = "a"
            '--------------------------------------------------------------------

            Msg = "Value = " & Value
            Assert.IsTrue(MathLib.Algebra.Hexadecimal.IsHex(Value), Msg)

        End Sub

        <TestMethod()>
    <Description("A")> _
        Public Sub Test_12()

            Dim Msg As String
            Dim Value As String

            '-- Initialisation des valeurs --------------------------------------
            Value = "A"
            '--------------------------------------------------------------------

            Msg = "Value = " & Value
            Assert.IsTrue(MathLib.Algebra.Hexadecimal.IsHex(Value), Msg)

        End Sub

        <TestMethod()>
    <Description("b")> _
        Public Sub Test_13()

            Dim Msg As String
            Dim Value As String

            '-- Initialisation des valeurs --------------------------------------
            Value = "b"
            '--------------------------------------------------------------------

            Msg = "Value = " & Value
            Assert.IsTrue(MathLib.Algebra.Hexadecimal.IsHex(Value), Msg)

        End Sub

        <TestMethod()>
    <Description("B")> _
        Public Sub Test_14()

            Dim Msg As String
            Dim Value As String

            '-- Initialisation des valeurs --------------------------------------
            Value = "B"
            '--------------------------------------------------------------------

            Msg = "Value = " & Value
            Assert.IsTrue(MathLib.Algebra.Hexadecimal.IsHex(Value), Msg)

        End Sub

        <TestMethod()>
    <Description("c")> _
        Public Sub Test_15()

            Dim Msg As String
            Dim Value As String

            '-- Initialisation des valeurs --------------------------------------
            Value = "c"
            '--------------------------------------------------------------------

            Msg = "Value = " & Value
            Assert.IsTrue(MathLib.Algebra.Hexadecimal.IsHex(Value), Msg)

        End Sub

        <TestMethod()>
    <Description("C")> _
        Public Sub Test_16()

            Dim Msg As String
            Dim Value As String

            '-- Initialisation des valeurs --------------------------------------
            Value = "C"
            '--------------------------------------------------------------------

            Msg = "Value = " & Value
            Assert.IsTrue(MathLib.Algebra.Hexadecimal.IsHex(Value), Msg)

        End Sub

        <TestMethod()>
    <Description("d")> _
        Public Sub Test_17()

            Dim Msg As String
            Dim Value As String

            '-- Initialisation des valeurs --------------------------------------
            Value = "d"
            '--------------------------------------------------------------------

            Msg = "Value = " & Value
            Assert.IsTrue(MathLib.Algebra.Hexadecimal.IsHex(Value), Msg)

        End Sub

        <TestMethod()>
    <Description("D")> _
        Public Sub Test_18()

            Dim Msg As String
            Dim Value As String

            '-- Initialisation des valeurs --------------------------------------
            Value = "D"
            '--------------------------------------------------------------------

            Msg = "Value = " & Value
            Assert.IsTrue(MathLib.Algebra.Hexadecimal.IsHex(Value), Msg)

        End Sub

        <TestMethod()>
    <Description("e")> _
        Public Sub Test_19()

            Dim Msg As String
            Dim Value As String

            '-- Initialisation des valeurs --------------------------------------
            Value = "e"
            '--------------------------------------------------------------------

            Msg = "Value = " & Value
            Assert.IsTrue(MathLib.Algebra.Hexadecimal.IsHex(Value), Msg)

        End Sub

        <TestMethod()>
    <Description("E")> _
        Public Sub Test_20()

            Dim Msg As String
            Dim Value As String

            '-- Initialisation des valeurs --------------------------------------
            Value = "E"
            '--------------------------------------------------------------------

            Msg = "Value = " & Value
            Assert.IsTrue(MathLib.Algebra.Hexadecimal.IsHex(Value), Msg)

        End Sub

        <TestMethod()>
    <Description("f")> _
        Public Sub Test_21()

            Dim Msg As String
            Dim Value As String

            '-- Initialisation des valeurs --------------------------------------
            Value = "f"
            '--------------------------------------------------------------------

            Msg = "Value = " & Value
            Assert.IsTrue(MathLib.Algebra.Hexadecimal.IsHex(Value), Msg)

        End Sub

        <TestMethod()>
    <Description("F")> _
        Public Sub Test_22()

            Dim Msg As String
            Dim Value As String

            '-- Initialisation des valeurs --------------------------------------
            Value = "F"
            '--------------------------------------------------------------------

            Msg = "Value = " & Value
            Assert.IsTrue(MathLib.Algebra.Hexadecimal.IsHex(Value), Msg)

        End Sub

        <TestMethod()>
    <Description("AFC12E")> _
        Public Sub Test_23()

            Dim Msg As String
            Dim Value As String

            '-- Initialisation des valeurs --------------------------------------
            Value = "AFC12E"
            '--------------------------------------------------------------------

            Msg = "Value = " & Value
            Assert.IsTrue(MathLib.Algebra.Hexadecimal.IsHex(Value), Msg)

        End Sub

        <TestMethod()>
    <Description("1.A")>
        Public Sub Test_24()

            Dim Msg As String
            Dim Value As String

            '-- Initialisation des valeurs --------------------------------------
            Value = "1.A"
            '--------------------------------------------------------------------

            Msg = "Value = " & Value
            Assert.IsTrue(MathLib.Algebra.Hexadecimal.IsHex(Value), Msg)

        End Sub

        <TestMethod()>
    <Description("a12.B15")>
        Public Sub Test_25()

            Dim Msg As String
            Dim Value As String

            '-- Initialisation des valeurs --------------------------------------
            Value = "a12.B15"
            '--------------------------------------------------------------------

            Msg = "Value = " & Value
            Assert.IsTrue(MathLib.Algebra.Hexadecimal.IsHex(Value), Msg)

        End Sub

        <TestMethod()>
    <Description("-A12")>
        Public Sub Test_26()

            Dim Msg As String
            Dim Value As String

            '-- Initialisation des valeurs --------------------------------------
            Value = "-A12"
            '--------------------------------------------------------------------

            Msg = "Value = " & Value
            Assert.IsTrue(MathLib.Algebra.Hexadecimal.IsHex(Value), Msg)

        End Sub

        <TestMethod()>
    <Description("-A12.c10")>
        Public Sub Test_27()

            Dim Msg As String
            Dim Value As String

            '-- Initialisation des valeurs --------------------------------------
            Value = "-A12.c10"
            '--------------------------------------------------------------------

            Msg = "Value = " & Value
            Assert.IsTrue(MathLib.Algebra.Hexadecimal.IsHex(Value), Msg)

        End Sub

        <TestMethod()>
    <Description("")>
        Public Sub Test_28()

            Dim Msg As String
            Dim Value As String

            '-- Initialisation des valeurs --------------------------------------
            Value = ""
            '--------------------------------------------------------------------

            Msg = "Value = " & Value
            Assert.IsFalse(MathLib.Algebra.Hexadecimal.IsHex(Value), Msg)

        End Sub

        <TestMethod()>
    <Description("AZ")>
        Public Sub Test_29()

            Dim Msg As String
            Dim Value As String

            '-- Initialisation des valeurs --------------------------------------
            Value = "AZ"
            '--------------------------------------------------------------------

            Msg = "Value = " & Value
            Assert.IsFalse(MathLib.Algebra.Hexadecimal.IsHex(Value), Msg)

        End Sub

        <TestMethod()>
    <Description("--A")>
        Public Sub Test_30()

            Dim Msg As String
            Dim Value As String

            '-- Initialisation des valeurs --------------------------------------
            Value = "--A"
            '--------------------------------------------------------------------

            Msg = "Value = " & Value
            Assert.IsFalse(MathLib.Algebra.Hexadecimal.IsHex(Value), Msg)

        End Sub

        <TestMethod()>
    <Description("A.12.B")>
        Public Sub Test_31()

            Dim Msg As String
            Dim Value As String

            '-- Initialisation des valeurs --------------------------------------
            Value = "A.12.B"
            '--------------------------------------------------------------------

            Msg = "Value = " & Value
            Assert.IsFalse(MathLib.Algebra.Hexadecimal.IsHex(Value), Msg)

        End Sub

        <TestMethod()>
    <Description("12-A")>
        Public Sub Test_32()

            Dim Msg As String
            Dim Value As String

            '-- Initialisation des valeurs --------------------------------------
            Value = "12-A"
            '--------------------------------------------------------------------

            Msg = "Value = " & Value
            Assert.IsFalse(MathLib.Algebra.Hexadecimal.IsHex(Value), Msg)

        End Sub

        <TestMethod()>
    <Description("-A-2")>
        Public Sub Test_33()

            Dim Msg As String
            Dim Value As String

            '-- Initialisation des valeurs --------------------------------------
            Value = "-A-2"
            '--------------------------------------------------------------------

            Msg = "Value = " & Value
            Assert.IsFalse(MathLib.Algebra.Hexadecimal.IsHex(Value), Msg)

        End Sub

    End Class

End Namespace
